import typescript from '@rollup/plugin-typescript';
import json       from '@rollup/plugin-json';

export default {
  input: './src/index.tsx',
  plugins: [
    typescript(),
    json()
  ],
  output: [
    {
        format: 'cjs',
        file: './bin/runtime.js'
    },
]
}