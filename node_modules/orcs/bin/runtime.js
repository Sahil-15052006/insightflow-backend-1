'use strict';

var React = require('react');
var blessed = require('blessed');
var reactBlessed = require('react-blessed');
var commander = require('commander');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var React__default = /*#__PURE__*/_interopDefaultLegacy(React);
var blessed__default = /*#__PURE__*/_interopDefaultLegacy(blessed);

// import blessed from 'blessed';
const Sidebar = () => React__default['default'].createElement("blessed-box", { top: "left", left: "0%", width: "25%", height: "100%", border: { type: 'line' }, style: { border: { fg: 'blue' }
    } }, "Sidebar");

const Viewer = () => React__default['default'].createElement("blessed-box", { top: "left", left: "25%", width: "75%", height: "100%", border: { type: 'line' }, style: { border: { fg: 'red' }
    } }, "viewer");

const DashboardContainer = () => React__default['default'].createElement(React__default['default'].StrictMode, null,
    React__default['default'].createElement(Sidebar, null),
    React__default['default'].createElement(Viewer, null));

const cmd = new commander.Command();
cmd
    .command('config')
    .description('help help help')
    .action(cmd => {
    // Creating our screen
    const screen = blessed__default['default'].screen({
        autoPadding: true,
        smartCSR: true,
        title: 'logger'
    });
    // Adding a way to quit the program
    screen.key(['escape', 'q', 'C-c'], function (ch, key) {
        return process.exit(0);
    });
    // Rendering the React app using our screen
    reactBlessed.render(React__default['default'].createElement(DashboardContainer, null), screen);
});
cmd.parse();
