// check empty dataset
// check reqired colmns
// check obj is null or not 
// check null in row
// check type
// check duplicates

export default function DataValidater(cleanedData,schema){
    if (cleanedData.length <= 0) return
    
    cleanedData.forEach(row => {
            
    });
}